# Site claim + redirect suites (GitLab #332).
#
# The truth pins in test/ (public-claim accuracy, compliance-claim refusal,
# redirect contract) only prevent anything if they run on every change.
# Dependency-free node:test — no install step.
#
# External actions are pinned to immutable commit SHAs; workflow
# permissions are least-privilege (read-only contents).

name: Site tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  site-tests:
    name: Claim and redirect suites
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
      - name: Run the suites
        run: node --test 'test/*.test.js'
      - name: Build the site (allowlist build must stay runnable)
        run: bash scripts/build-site.sh

  a11y:
    name: Accessibility (axe-core, WCAG 2.2 AA)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      # Only two dev dependencies, both pinned in package-lock.json:
      # playwright-core (no bundled browsers) and axe-core.
      - name: Install
        run: npm ci
      # ubuntu-latest ships Google Chrome, so playwright-core drives it
      # directly and nothing downloads a browser at CI time.
      - name: Locate Chrome
        run: |
          which google-chrome || which google-chrome-stable
          google-chrome --version || google-chrome-stable --version
          echo "CHROMIUM_PATH=$(which google-chrome || which google-chrome-stable)" >> "$GITHUB_ENV"
      - name: Run the accessibility gate
        run: npm run test:a11y

  layout:
    name: Responsive layout (browser geometry)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci
      - name: Locate Chrome
        run: |
          which google-chrome || which google-chrome-stable
          echo "CHROMIUM_PATH=$(which google-chrome || which google-chrome-stable)" >> "$GITHUB_ENV"
      # The a11y gate proves semantics and contrast; this one proves
      # geometry — no text painted through text, nothing clipped, the
      # navigation genuinely usable at phone widths (#370).
      - name: Run the responsive-layout gate
        run: npm run test:layout

  webkit:
    name: WebKit scroll stability (iPhone-width)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci
      # The #371 flicker only ever reproduced under WebKit — the engine on
      # every iPhone. playwright-core drives the browser; the binary comes
      # from the matching playwright CLI at CI time.
      - name: Install WebKit
        run: npx playwright@1.62.1 install --with-deps webkit
      - name: Run the WebKit stability gate
        run: npm run test:webkit
